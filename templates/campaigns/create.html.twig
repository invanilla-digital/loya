{% extends 'layouts/base.html.twig' %}

{% block title %}Create Campaign{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li>
        <a href="{{ path('campaigns_index') }}">Campaigns</a>
    </li>
    <li class="is-active">
        <a href="{{ path('campaigns_create') }}" aria-current="page">Create</a>
    </li>
{% endblock %}

{% block body %}
    <div class="columns">
        <div class="container">
            <article class="panel has-background-white">
                <p class="panel-heading">
                    Create campaign
                </p>
                <div class="panel-block">
                    <div class="column">
                        {{ form(form) }}
                    </div>
                </div>
            </article>
        </div>
    </div>
{% endblock %}


